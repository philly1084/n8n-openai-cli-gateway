apiVersion: v1
kind: Service
metadata:
  name: n8n-openai-cli-gateway
  labels:
    app: n8n-openai-cli-gateway
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: n8n-openai-cli-gateway
